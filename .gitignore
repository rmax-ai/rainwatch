# Dependencies
node_modules/

# Build output
dist/

# Test output
coverage/

# Runtime data storage (SPEC §22: /data is the runtime lifecycle store —
# raw/, processed/, snapshots/). Root-anchored: MUST NOT match apps/data/.
# Never commit downloaded source files, processed intermediates, or snapshots.
/data/

# Secrets and local environment
.env
.envrc

# Logs
*.log

# macOS
.DS_Store
.serena/
